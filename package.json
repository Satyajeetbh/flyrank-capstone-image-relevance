{
  "name": "flyrank-capstone-image-relevance",
  "version": "1.0.0",
  "description": "Foundation for the FlyRank AI image understanding and content matching engine",
  "main": "dist/index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "build": "tsc --pretty",
    "typecheck": "tsc --noEmit --pretty",
    "verify:db": "npm run build && node scripts/verify-database.mjs",
    "verify:repositories": "npm run build && node scripts/verify-repositories.mjs",
    "test:unit": "npm run build && node --test tests/unit/mismatch-guard.test.mjs"
  },
  "keywords": [
    "typescript",
    "image-relevance"
  ],
  "author": "FlyRank",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/pg": "^8.23.1",
    "typescript": "^7.0.2"
  }
}
